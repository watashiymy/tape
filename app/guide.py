"""「使用说明」页与就地帮助的全部文案（v0.2.1 设计 §3）。

**为什么全部塞进一个模块**：说明里的数字（扫描池多大、跑一次多久、策略实测收益率）
是用户会照着做判断的事实。散在 dashboard.py 各处就没法整体对账，编错一个百分点没人看得出来
——比崩页更糟，崩页看得见，假数字看不见。集中在这里之后 tests/test_guide.py
才能做到三件事：逐条与 README 对账、验算推导出来的结论（"回撤只有约 1/3"）、
检查没有任何一处就地硬写百分数。

**纯数据 + 纯字符串**：不依赖 streamlit、不碰文件、不碰进程。面板只负责把这些串
塞进 st.markdown / st.warning / help= 里。

**数字的唯一来源是 FACTS**，正文一律用 `{占位符}` 取值（见 `_f`）。
FACTS 的每一项都必须在 README.md 里逐字出现，改一边不改另一边测试就红。
"""
from __future__ import annotations

from dataclasses import dataclass

# README 里对应这份说明的一节；tests 按它定位那一节做数字对账。
README_SECTION = "面板使用说明页"

# ---------------------------------------------------------------- 实测事实
# 回测那一组来自 README「① 回测」的实测表格（区间 2016-01-04 ~ 2026-08-24，
# 10 只主板蓝筹、本金 500 万、含全部成本）；运行耗时那一组来自 README §4 的
# 实测表格，出处是 output/runs/ 里的真实日志与状态文件。
#
# 一律写成字符串而不是数字：这些值要原样进 markdown，float 化再格式化只会
# 引入一层"格式串写错就显示成 117.40000001%"的风险，而且 README 对账要逐字比。
FACTS: dict[str, str] = {
    # —— 双均线 ma_cross
    "ma_total": "117.4%", "ma_cagr": "7.6%", "ma_dd": "-15.2%",
    "ma_sharpe": "0.97", "ma_trades": "243",
    # —— 唐奇安 donchian
    "dc_total": "49.3%", "dc_cagr": "3.8%", "dc_dd": "-12.4%",
    "dc_sharpe": "0.57", "dc_trades": "436",
    # —— 主要对照组：等权买入持有（后复权价、分红再投资口径，与策略同权）
    "hold_total": "412.5%", "hold_cagr": "16.6%", "hold_dd": "-40.4%",
    # —— 次要对照：沪深300 价格指数（不含成分股分红，别拿它当主参照）
    "csi300_total": "31.5%", "csi300_cagr": "2.6%", "csi300_dd": "-45.6%",
    # —— 回测区间与本金
    "backtest_start": "2016-01-04", "backtest_end": "2026-08-24",
    "universe_n": "10", "capital": "500 万",
    # —— 运行耗时与规模（实测日志）
    "scan_pool": "3010 只", "scan_seconds": "1048 秒", "scan_minutes": "约 18 分钟",
    "scan_signals": "110 条", "scan_cold": "0.5–2 小时",
    "backtest_seconds": "10 秒",
    "liquidity_floor": "5000 万元",
    # —— 安全：把面板绑到回环地址的那个开关。**不是**可选的美化项：
    # 实测 streamlit run 不带地址参数时监听 *:8501（所有网卡），而面板能执行本机命令。
    "bind_flag": "--server.address 127.0.0.1", "default_port": "8501",
    # —— 时间与门槛
    "data_ready": "17:30", "safe_time": "18:30", "limit_max": "10000",
    "slippage": "0.1%", "rf_real": "1.5–2%", "csi300_drag": "2–3%",
}


def _f(text: str) -> str:
    """把正文里的 `{占位符}` 换成 FACTS 的值。

    占位符拼错会当场 KeyError（模块 import 时就炸），比渲染出一个字面
    `{ma_total}` 好得多——后者在页面上像个排版故障，没人会当成数据错误上报。
    """
    return text.format(**FACTS)


@dataclass(frozen=True)
class Section:
    """说明页的一节。

    `key` 是稳定标识：标题文案随时会改，测试与面板都按 key 定位。
    `flow` 非空时在正文上方画一张闭环图（theme.flow）。
    `emphasis` 只给安全提示——面板据此改用 st.warning 单独成块（§3.1 第 7 条）。
    到处都是黄框等于没有黄框，所以有测试钉住"只有一节 emphasis"。
    """
    key: str
    title: str
    body: str
    flow: tuple[str, ...] = ()
    emphasis: bool = False


# ---------------------------------------------------------------- 页头那句话
PAGE_INTRO = _f("面板自带的手册：三个任务分别干什么、输出怎么读、"
                "已知局限在哪。第一次用先看这页（约五分钟）。")


# ---------------------------------------------------------------- 八节正文（§3.1）

_TASKS = _f("""三个任务的分工是**发现 / 跟踪 / 检验**，缺一个闭环就断了。

| 任务 | 干什么 | 只报什么 | 结果看哪儿 |
|---|---|---|---|
| **全市场扫描** | 固定池之外的雷达：扫全部沪深主板非 ST 股票（实测扫描池 {scan_pool}），按当日成交额降序 | **只报当日新触发的 BUY**——全市场的 SELL 对没持仓的人毫无意义 | 「今日信号」页页尾的扫描区块 |
| **每日信号** | 只跟踪 `config/settings.yaml` 里的固定池（当前 {universe_n} 只），与前一交易日比对 | 新增的 **BUY 和 SELL** | 「今日信号」页 |
| **回测** | 同一套策略在历史上跑一遍，出绩效指标、净值报告与逐笔成交 | 历史检验结论，**不产生今天的信号** | 「回测报告」「个股K线」两页 |

**闭环怎么走**（就是上面那行图）：扫描只是发现工具，它自己不下结论。看中的票必须
**手工加进 `config/settings.yaml` 的 `universe`**，从此它的**卖出**信号才由「每日信号」
每天替你盯着。

> **不加进 `universe` 的票，没有任何人管它的卖出。** 这是本项目最容易踩的坑：
> 扫描报了 BUY，人买进去了，然后再也没有任何提示——因为「每日信号」只看固定池。
""")

_WORKFLOW = _f("""1. **收盘之后**再跑。baostock 的当日日线约 **{data_ready}** 之后才更新，
   早跑会因"全部标的数据未更新"直接退出（退出码 1）；实践中 **{safe_time}** 之后最稳。
2. 去「任务控制台」点「全市场扫描」的 ▶ 开始。**第一次先把 `--limit` 填 300 试跑**，
   确认环境正常再上全量——全量是挂机任务，不是等一会儿就好的那种。
3. 扫完在卡片里直接看信号表，或去「今日信号」页页尾看。**核对标题里的扫描日期**
   是不是你要的那天（停牌日、忘跑的日子最容易把上周的当成今天的）。
4. 挑出想跟的票 → 编辑 `config/settings.yaml` 的 `universe` → 保存。
5. 此后**每天**收盘后跑一次「每日信号」，买卖提示都从这里出。
6. 改了策略参数就跑一次「回测」，看这套参数在历史上是什么形状。

**各任务耗时（实测，不是估计）**

| 任务 | 实测 | 备注 |
|---|--:|---|
| 全市场扫描（全量、缓存已热） | {scan_seconds}（**{scan_minutes}**） | {scan_pool}，报出 {scan_signals} 信号。**首次冷缓存要慢得多**，按 {scan_cold}准备：缓存省的是不重拉历史窗口，省不掉每票一次联网往返 |
| 回测（两个策略、缓存已热） | **{backtest_seconds}** | 首次要联网拉 {universe_n} 只标的约十年日线，会久一些 |
| 每日信号 | 十几秒**量级** | 只增量补当日数据。数据没更新时约 4 秒就明确退出 |

任务跑在**独立进程**里：关掉浏览器、甚至停掉本面板都不会中断它，回来还能看到进度。
同时只允许一个任务（baostock 是单会话，并发会互踢下线，还更慢）。
""")

_SCAN = _f("""| 列 | 含义 | 怎么用 |
|---|---|---|
| 日期 | 信号所属的**交易日** | 先核对它，不一定是今天 |
| 代码 / 名称 | 标的 | 代码按字符串处理，`000333` 的前导零不会被吃掉 |
| 策略 | 哪个策略报的 | 两个策略同时报同一只，只说明两套规则今天都满足，**不代表信号更强** |
| 收盘价 | 当日收盘价 | — |
| 涨跌幅% | 当日涨跌幅 | **参考价值最低的一列。** 机械信号看的是突破与量，不是今天涨了几个点 |
| 成交额 | 当日成交额（元） | 扫描已按 20 日均额 ≥ {liquidity_floor}（`scan.min_avg_amount`）滤掉低流动性标的 |
| **放量倍数** | 当日成交额 ÷ 前 20 日均额 | **最该看的一列**，也是表里唯一画成进度条的一列 |

**为什么放量倍数排在涨跌幅前面**：突破要靠量确认。没有量的新高多半是**假突破**
——盘口薄、几笔就能顶上去，第二天原样掉回来。涨跌幅只告诉你"今天涨了"，
放量倍数才告诉你"有多少钱在参与"。

**两个机械规则看不出来的陷阱**

- **同板块的多只信号，本质是同一笔押注。** 一次扫描报了 8 只银行，那不是 8 个
  独立机会，是"银行今天集体动了"这**一件事**。全买等于把仓位全押在一个 beta 上，
  风险不是分散了而是**集中了**。扫描按成交额排序，完全不知道行业。
- **扫出来 ≠ 策略在这只票上历史有效。** 回测只跑固定 `universe` 那 {universe_n} 只。
  某只票被扫出来，含义仅仅是"技术条件今天满足了"，没有任何历史背书。
- 那次全量实测在 {scan_pool} 里报了 {scan_signals}。这不是选股结论，是**待研究清单**。
""")

_METRICS = _f("""| 指标 | 含义 | 读的时候注意 |
|---|---|---|
| 总收益率 | 期末净值 / 期初 − 1 | 单独看没意义，**必须**和同期「等权买入持有」比 |
| 年化收益率 | 日历年化（首末跨度 ÷ 365.25） | 是几何平均，不是"每年都能有这么多" |
| 最大回撤 | 净值从历史高点跌下来的最大幅度 | **恒为负**，表里是绿色。这是"能不能睡着"的指标，比收益更决定你拿不拿得住 |
| 夏普比率(rf=0) | 年化收益 / 年化波动 | 无风险利率按 0 算（简化口径），中国实际约 {rf_real}，所以这里的夏普**偏高** |
| 交易次数 | 已成交笔数（买 + 卖） | 次数多不等于勤奋，等于**成本高**：每笔都吃佣金、印花税与 {slippage} 滑点 |
| 胜率 | 盈利平仓笔数 / 已平仓笔数 | 只统计**已平仓**的。40% 出头很正常——趋势跟随靠少数大赢单赚钱 |
| 盈亏比 | 总盈利 / 总亏损（取绝对值） | 必须和胜率配对看：胜率 40% + 盈亏比 2.5 是健康的，胜率 40% + 盈亏比 1.0 是稳定亏钱 |
| 平均持仓天数 | **自然日**，不是交易日 | 双均线明显长于唐奇安，那就是"迟钝"与"急躁"的数字形态 |

**本项目的实测结论（如实写，这是诚实结果不是 bug）**

区间 {backtest_start} ~ {backtest_end}，{universe_n} 只主板蓝筹，本金 {capital}，含全部成本：

| | 总收益 | 年化 | 最大回撤 | 夏普(rf=0) | 交易次数 |
|---|--:|--:|--:|--:|--:|
| 双均线 ma_cross | {ma_total} | {ma_cagr} | {ma_dd} | {ma_sharpe} | {ma_trades} |
| 唐奇安 donchian | {dc_total} | {dc_cagr} | {dc_dd} | {dc_sharpe} | {dc_trades} |
| **等权买入持有** | **{hold_total}** | **{hold_cagr}** | **{hold_dd}** | — | — |
| 沪深300（价格指数） | {csi300_total} | {csi300_cagr} | {csi300_dd} | — | — |

**两个策略都大幅跑输"买入后躺平"**（{ma_total} / {dc_total} vs {hold_total}），
但最大回撤只有它的**约 1/3**（{ma_dd} / {dc_dd} vs {hold_dd}）。

**为什么，以及为什么这不是 bug**：这 {universe_n} 只是**十年长牛的蓝筹**，
恰恰是对趋势跟随**最不利**的样本——每一次震荡都把持仓洗出场，而躺平的人一次都没被洗掉。
换个说法：这组数字证明的是"这条流水线跑得通、且数是对的"，不是"这两个策略不行"。
拿它去推断策略优劣，方向就错了。

沪深300 只有 {csi300_total}，是因为它是**价格指数、不含成分股分红**，每年天然吃亏
{csi300_drag}——**不要拿它当主要参照**。主对照组是用后复权价（分红再投资口径）算的
「等权买入持有」，与策略同权，才是公平的那一栏。
""")

_STRATEGIES = _f("""| | 双均线 ma_cross | 唐奇安通道突破 donchian |
|---|---|---|
| 进场看什么 | 短均线**站上**长均线（看两条线的**相对位置**） | 收盘**创 N 日新高**（看**突破**） |
| 看不看量 | **不看** | **必须放量**才算，量不够的突破直接不认 |
| 反应速度 | 迟钝：均线得先拐过来，往往涨了一截才进场 | 快：突破当天就进 |
| 出场 | 短均线**跌回**长均线下方，同样迟钝 | 跌破 M 日新低就走，很急 |
| 实测交易次数 | {ma_trades} 笔 | {dc_trades} 笔——差不多是前者两倍，成本也是 |
| 性格 | 拿得久、被洗得少，但入场晚、回撤来了走得慢 | 抓得快，也被**假突破**打得多 |

一句话对比：**双均线是慢性子，唐奇安是急性子**。同一段行情里，前者少做多拿、
后者多做少拿；实测总收益前者高（{ma_total} vs {dc_total}），但回撤前者反而略深
（{ma_dd} vs {dc_dd}），因为它出场慢。

参数都在 `config/settings.yaml` 的 `strategies` 段，改完跑一次回测就能看到形状变化。
**注意**：这两组参数**没有做过调优也没做样本外检验**（无参数扫描、无前进分析），
现有数字只是"流水线跑得通"的证据，不是策略有效性的证据。
""")

_LIMITS = _f("""看结果之前先看这里。下面每一条都会让数字偏离真实交易：

- **涨跌停判定是近似**：按「开盘价 ≥ 昨收 × 1.095（或 ≤ 0.905）」或「当日最高 == 最低
  的一字板」推断。真实盘口的排队、封板中途打开、部分成交都复现不了。
- **除权除息按"分红再投资"处理**：除权日持仓股数 × (今日因子 / 昨日因子)，现金不变。
  于是**持仓股数会变成非整数**，与真实账户"现金分红入账、股数不变"有出入。
- **夏普按 rf = 0 算**（简化口径），中国无风险利率约 {rf_real}，真实夏普比这里低。
- **沪深300 是价格指数、不含成分股分红**，每年天然劣势 {csi300_drag}，别拿它当主要参照。
- **扫描池只有沪深主板非 ST**：创业板/科创板 20%、ST 的 5% 涨跌幅分支都没建模，
  所以刻意不扫，免得算出一堆不成立的信号。
- **成交假设过于友好**：以次日开盘价 ± {slippage} 滑点**全额**成交，不考虑冲击成本、
  流动性不足与部分成交。
- **股票池有幸存者偏差**：这 {universe_n} 只是**今天**回头挑出来的白马，
  2016 年时并不知道它们会走成这样；退市股、掉队股一只都没有，回测收益天然偏乐观。
- **胜率 / 盈亏比 / 平均持仓天数只统计已平仓交易**，回测末尾仍持仓的那笔不计入。
- **数据没有与交易所对账**：baostock 是免费源，复权因子、停牌标记、成交额都按它给的用。

完整清单（含 ST 的 5% 未建模、现金不计利息、夏普年化 √252 的偏差等）见 README 的
「已知近似与局限」一节。
""")

_SAFETY = _f("""**这个面板能在本机执行脚本**——「开始」按钮起的是真进程，等于一个本地命令执行入口。

- **启动时必须显式绑定回环地址**：

  ```bash
  .venv/bin/streamlit run app/dashboard.py {bind_flag}
  ```

  **不要指望默认值是安全的。** 实测（`lsof -nP -iTCP:{default_port} -sTCP:LISTEN`）：
  `streamlit run` 不带地址参数时监听的是 `*:{default_port}`，也就是**所有网卡**
  ——启动日志里那两行 `Network URL` / `External URL` 就是证据。同一网段的人打开这个
  地址就能点你的「开始」按钮，等于能在你的机器上执行命令。加上 `{bind_flag}` 之后
  监听项变成 `127.0.0.1:{default_port}`，那两行 URL 也随之消失。
- 更不要用 `--server.address 0.0.0.0` 或任何内网地址——那是把上面这件事从疏忽变成主动邀请。
- 面板刻意**不暴露 `--config`**（避免任意路径读取），任务一律用 `config/settings.yaml`。
- 参数走白名单：日期必须是 `YYYY-MM-DD` 的真实日期、`--limit` 是 1–{limit_max} 的正整数、
  策略只能从注册表里挑；命令一律以**参数列表**方式执行，**不经过 shell**。
- 策略与信号仅用于学习，**不构成任何投资建议**。回测是历史模拟，含上面那一堆近似。
""")

_CLI = _f("""面板上的按钮与下面这些命令**完全等价**——面板起的就是它们。
所有命令都要在**项目根目录**执行（脚本里的路径都是相对的）。

```bash
# ① 全市场扫描（收盘后跑）
.venv/bin/python scripts/run_market_scan.py                     # 全量，{scan_minutes}
.venv/bin/python scripts/run_market_scan.py --limit 300         # 只扫前 300 只（试跑）
.venv/bin/python scripts/run_market_scan.py --date 2026-08-21   # 补扫某个历史交易日

# ② 每日信号（收盘后跑）
.venv/bin/python scripts/run_daily_signal.py

# ③ 回测
.venv/bin/python scripts/run_backtest.py                        # 全部策略，{backtest_seconds}
.venv/bin/python scripts/run_backtest.py --strategy donchian    # 只跑一个
.venv/bin/python scripts/run_backtest.py --refresh              # 丢弃缓存全量重拉
```

两点差别要知道：

- 命令行起的任务**不写** `output/runs/`，面板看不到它的进度（产物照样出现在页面上）；
- 因此它**不参与面板那条互斥**。终端已经在跑扫描时别再去面板点「开始」，
  否则两个 baostock 会话互踢下线。要么全在面板点，要么全在终端跑。

面板起的任务日志留在 `output/runs/logs/<run_id>.log`，**不自动清理**（留着事后排查）。
""")

SECTIONS: tuple[Section, ...] = (
    Section("tasks", "三个任务分别是什么", _TASKS,
            flow=("全市场扫描：发现新 BUY", "人工研究",
                  "加进 settings.yaml 的 universe", "每日信号：每天跟踪买/卖")),
    Section("workflow", "典型工作流", _WORKFLOW),
    Section("scan", "怎么读扫描结果", _SCAN),
    Section("metrics", "怎么读回测指标（含本项目实测结论）", _METRICS),
    Section("strategies", "两个内置策略的思路对比", _STRATEGIES),
    Section("limits", "已知局限", _LIMITS),
    Section("safety", "安全提示", _SAFETY, emphasis=True),
    Section("cli", "命令行等价用法", _CLI),
)

_BY_KEY = {s.key: s for s in SECTIONS}


def section(key: str) -> Section:
    """按 key 取一节。拼错必须 KeyError 响亮失败：返回 None 会在页面上渲染出
    一片空白，谁都不会注意到少了一节。"""
    return _BY_KEY[key]


# ---------------------------------------------------------------- 就地帮助（§3.2）

# 任务卡片右上角 st.popover("?") 的内容：这个任务做什么、大概多久、产物在哪。
# 参数含义不写在这里——那是每个控件自己 help= 的事（见 PARAM_HELP），
# 写两遍必然改一处漏一处。键必须与 quant.runner.jobs.JOBS 完全一致（有测试钉住）。
JOB_HELP: dict[str, str] = {
    "market_scan": _f("""**全市场扫描**——固定池之外的雷达。

- **做什么**：扫全部沪深主板非 ST 股票（实测扫描池 {scan_pool}），
  **只报当日新触发的 BUY**，按当日成交额降序。
- **多久**：全量缓存已热实测 {scan_seconds}（{scan_minutes}）；
  首次冷缓存按 {scan_cold}准备。当挂机任务，随时可以点停止。
- **什么时候跑**：收盘后，{data_ready} 之后（当日数据那时才更新）。
- **产物**：`output/scan/<交易日>.csv`（全量），也会显示在「今日信号」页页尾。
"""),
    "daily_signal": _f("""**每日信号**——跟踪固定池，这是唯一会提示你**卖出**的任务。

- **做什么**：只看 `config/settings.yaml` 的 `universe`（当前 {universe_n} 只），
  与前一交易日比对，报**新增**的 BUY / SELL。
- **多久**：通常十几秒（只增量补当日数据）——这一项是**量级估计**，不像另两个任务有实测样本。
- **什么时候跑**：收盘后，{data_ready} 之后；建议每个交易日都跑。
  「今日无新信号」是绝大多数日子的正常结果。
- **产物**：`output/signals/<交易日>.csv`，显示在「今日信号」页。
"""),
    "backtest": _f("""**回测**——历史检验，不产生今天的信号。

- **做什么**：拿配置里的策略在固定池上跑一遍历史，出绩效指标、净值报告与逐笔成交。
- **多久**：两个策略、缓存已热实测 {backtest_seconds}；
  首次要联网拉 {universe_n} 只标的约十年日线，会久一些。
- **什么时候跑**：改完 `config/settings.yaml` 的策略参数之后。不依赖当日数据，随时能跑。
- **产物**：`output/<策略名>_<时间戳>/`（metrics.json / report.html / trades.csv /
  kline_*.html 等），显示在「回测报告」与「个股K线」两页。
"""),
}

# 参数控件的 help= tooltip。键是 (任务名, 参数名)，必须覆盖 JOBS 里每个 Param
# （有测试钉住）。四个控件都能"留空"，而留空的语义各不相同——不写清楚，
# 用户第一次点开始就会误跑一次全量扫描。
PARAM_HELP: dict[tuple[str, str], str] = {
    ("market_scan", "limit"): _f(
        "只扫前 N 只，**试跑用**（先填 300 确认环境正常）。"
        "留空 = 全量 {scan_pool}，{scan_minutes}起。上限 {limit_max}。"),
    ("market_scan", "date"): _f(
        "以指定交易日为基准，用来补扫历史某天。留空 = 最近交易日。"
        "给非交易日、或当日 {data_ready} 前数据还没更新，都会明确提示后退出。"),
    ("backtest", "strategy"): _f(
        "只回测一个策略：ma_cross（双均线）或 donchian（唐奇安通道突破）。"
        "留空 = 跑配置里的**全部**策略，每个出一份独立报告。"),
    ("backtest", "refresh"): _f(
        "丢掉本地**缓存**、把 {universe_n} 只标的约十年日线全部**重拉**一遍。"
        "只在怀疑缓存脏了时勾——平时增量补最近几天就够，勾上会慢很多。"),
}

# 每张数据表格上方那一行灰字。只讲"这张表里哪一列最该看 / 哪一列为什么是空的"，
# 逐列解释在说明页里，这里越短越好。
TABLE_HINTS: dict[str, str] = {
    "trades": "金额与股数已加千分位、数字列右对齐。盈亏只在**平仓**那一笔上有值，"
              "买入行与仍持仓的标的是空值——不是数据丢了。",
    "skipped": "被引擎约束挡掉的订单（涨跌停无法成交、T+1、资金不足一手等）。"
               "「信号有但没成交」先查这张表的原因列。",
    "scan": _f("最该看「放量倍数」（当日成交额 ÷ 前 20 日均额，画成进度条那列）"
               "——没有量的突破多半是假突破；涨跌幅的参考价值反而最低。"),
    "signal": "方向列 buy = 进场信号、sell = 出场信号；只有固定池 `universe` 里的"
              "标的才会出现在这里（扫描发现的票不加进池子就没人管卖出）。",
}

# 空态文案（§3.2）：按钮在哪 + 等价命令 + 预计耗时。
# "暂无数据"四个字什么也没告诉人，用户下一步只能去猜是坏了还是没跑过。
EMPTY_STATES: dict[str, str] = {
    "backtest": _f("暂无回测结果 —— 点上方控制条的「▶ 开始」，"
                   "或在项目根目录跑 `python scripts/run_backtest.py`"
                   "（缓存已热约 {backtest_seconds}；首次要联网拉 {universe_n} 只标的"
                   "约十年日线，会久一些）。"),
    "signal": _f("暂无信号记录 —— 收盘后（{data_ready} 之后当日数据才更新）"
                 "点上方控制条的「▶ 开始」，"
                 "或跑 `python scripts/run_daily_signal.py`（约十几秒）。"),
    "scan": _f("暂无全市场扫描结果 —— 收盘后点上方控制条「全市场扫描」的「▶ 开始」，"
               "或跑 `python scripts/run_market_scan.py`"
               "（全量 {scan_pool}，缓存已热实测 {scan_minutes}，"
               "冷缓存按 {scan_cold}准备；先用 `--limit 300` 试跑更稳）。"),
}
