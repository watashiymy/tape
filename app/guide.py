"""「使用说明」页与就地帮助的全部文案（v0.2.1 设计 §3）。

**为什么全部塞进一个模块**：说明里的数字（扫描池多大、跑一次多久、策略实测收益率）
是用户会照着做判断的事实。散在 dashboard.py 各处就没法整体对账，编错一个百分点没人看得出来
——比崩页更糟，崩页看得见，假数字看不见。集中在这里之后 tests/test_guide.py
才能做到三件事：逐条与 README 对账、验算推导出来的结论（"回撤只有约 1/3"）、
检查没有任何一处就地硬写百分数。

**纯数据 + 纯字符串**：不依赖 streamlit、不碰文件、不碰进程。面板只负责把这些串
塞进 st.markdown / st.warning / help= 里。

**数字的唯一来源是 FACTS**，正文一律用 `{占位符}` 取值（见 `_f`）。
FACTS 的每一项都必须在 README.md 里逐字出现，改一边不改另一边测试就红。
"""
from __future__ import annotations

from dataclasses import dataclass

# README 里对应这份说明的一节；tests 按它定位那一节做数字对账。
README_SECTION = "面板使用说明页"

# ---------------------------------------------------------------- 实测事实
# 回测那一组来自 README「① 回测」的实测表格（区间 2016-01-04 ~ 2026-08-24，
# 10 只主板蓝筹、本金 500 万、含全部成本）；运行耗时那一组来自 README §4 的
# 实测表格，出处是 output/runs/ 里的真实日志与状态文件。
#
# 一律写成字符串而不是数字：这些值要原样进 markdown，float 化再格式化只会
# 引入一层"格式串写错就显示成 117.40000001%"的风险，而且 README 对账要逐字比。
FACTS: dict[str, str] = {
    # —— 双均线交叉 ma_cross
    "ma_total": "117.4%", "ma_cagr": "7.6%", "ma_dd": "-15.2%",
    "ma_sharpe": "0.97", "ma_trades": "243",
    # —— 唐奇安通道突破 donchian
    "dc_total": "49.3%", "dc_cagr": "3.8%", "dc_dd": "-12.4%",
    "dc_sharpe": "0.57", "dc_trades": "436",
    # —— 主要对照组：等权买入持有（后复权价、分红再投资口径，与策略同权）
    "hold_total": "412.5%", "hold_cagr": "16.6%", "hold_dd": "-40.4%",
    # —— 次要对照：沪深300 价格指数（不含成分股分红，别拿它当主参照）
    "csi300_total": "31.5%", "csi300_cagr": "2.6%", "csi300_dd": "-45.6%",
    # —— 回测区间与本金
    "backtest_start": "2016-01-04", "backtest_end": "2026-08-24",
    "universe_n": "10", "capital": "500 万",
    # —— v0.4 当前默认口径（三策略 + 双叠加层开；2026-09-01 真实重跑，
    #     同池同本金，终点更新到 v4_end）。基线那组键**永不重算**，历史结论留档。
    "v4_end": "2026-08-31",
    "v4_ma_total": "8.6%", "v4_ma_cagr": "0.8%", "v4_ma_dd": "-15.2%",
    "v4_ma_sharpe": "0.15", "v4_ma_trades": "368",
    "v4_dc_total": "16.6%", "v4_dc_cagr": "1.5%", "v4_dc_dd": "-14.1%",
    "v4_dc_sharpe": "0.26", "v4_dc_trades": "412",
    "v4_ts_total": "6.5%", "v4_ts_cagr": "0.6%", "v4_ts_dd": "-17.4%",
    "v4_ts_sharpe": "0.14", "v4_ts_trades": "457",
    "v4_hold_total": "414.4%", "v4_hold_cagr": "16.6%", "v4_hold_dd": "-40.4%",
    "v4_csi300_total": "33.3%", "v4_csi300_cagr": "2.7%", "v4_csi300_dd": "-45.6%",
    # —— 叠加层归因（同池同区间的对照重跑，每格一次完整回测，总收益）
    "v4_ma_base": "118.5%", "v4_ma_gate": "78.9%", "v4_ma_stop": "25.8%",
    "v4_dc_base": "49.0%", "v4_dc_gate": "16.7%", "v4_dc_stop": "49.5%",
    "v4_ts_base": "87.0%", "v4_ts_gate": "74.1%", "v4_ts_stop": "1.0%",
    # 止损封锁的实测极值：tsmom 单开止损时，卖出到再入场的最长空窗（600519）
    "v4_lockout_max": "866 个自然日",
    # tsmom 无叠加层口径的最大回撤——三策略里唯一被止损真正压下来的那个，
    # 文案里"只有 tsmom 真降了"的前半截数字
    "v4_ts_base_dd": "-26.6%",
    # —— 运行耗时与规模（实测日志）
    "scan_pool": "3010 只", "scan_seconds": "1048 秒", "scan_minutes": "约 18 分钟",
    # 拉一次全市场清单（get_all_symbols，约 11500 行分页）的固定开销，
    # 出处与 README「③ 全市场扫描」的耗时拆解同一处实测（其中一次实测 133 秒）。
    # 信号池页的「加载可选标的清单」花的就是这笔钱，必须提前告诉用户。
    "pool_fetch": "2–4 分钟",
    "scan_signals": "110 条", "scan_cold": "0.5–2 小时",
    "backtest_seconds": "10 秒",
    "liquidity_floor": "5000 万元",
    # —— 安全：把面板绑到回环地址的那个开关。**不是**可选的美化项：
    # 实测 streamlit run 不带地址参数时监听 *:8501（所有网卡），而面板能执行本机命令。
    "bind_flag": "--server.address 127.0.0.1", "default_port": "8501",
    # —— 时间与门槛
    "data_ready": "17:30", "safe_time": "18:30", "limit_max": "10000",
    "slippage": "0.1%", "rf_real": "1.5–2%", "csi300_drag": "2–3%",
}


def _f(text: str) -> str:
    """把正文里的 `{占位符}` 换成 FACTS 的值。

    占位符拼错会当场 KeyError（模块 import 时就炸），比渲染出一个字面
    `{ma_total}` 好得多——后者在页面上像个排版故障，没人会当成数据错误上报。
    """
    return text.format(**FACTS)


@dataclass(frozen=True)
class Section:
    """说明页的一节。

    `key` 是稳定标识：标题文案随时会改，测试与面板都按 key 定位。
    `flow` 非空时在正文上方画一张闭环图（theme.flow）。
    `emphasis` 只给安全提示——面板据此改用 st.warning 单独成块（§3.1 第 7 条）。
    到处都是黄框等于没有黄框，所以有测试钉住"只有一节 emphasis"。
    """
    key: str
    title: str
    body: str
    flow: tuple[str, ...] = ()
    emphasis: bool = False


# ---------------------------------------------------------------- 页头那句话
PAGE_INTRO = _f("面板自带的手册：三个任务分别干什么、输出怎么读、"
                "已知局限在哪。第一次用先看这页（约五分钟）。")


# ---------------------------------------------------------------- 八节正文（§3.1）

_TASKS = _f("""三个任务的分工是**发现 / 跟踪 / 检验**，缺一个闭环就断了。

| 任务 | 干什么 | 只报什么 | 结果看哪儿 |
|---|---|---|---|
| **全市场扫描** | 固定池之外的雷达：扫全部沪深主板非 ST 股票（实测扫描池 {scan_pool}），按当日成交额降序 | **只报当日新触发的 BUY**——全市场的 SELL 对没持仓的人毫无意义 | 「今日信号」页页尾的扫描区块 |
| **每日信号** | 只跟踪**信号池**里那批标的（在「信号池」页增删，落在本地 `config/universe.local.yaml`），与前一交易日比对 | 新增的 **BUY 和 SELL** | 「今日信号」页 |
| **回测** | 同一套策略在历史上跑一遍，出绩效指标、净值报告与逐笔成交 | 历史检验结论，**不产生今天的信号** | 「回测报告」「个股K线」两页 |

**闭环怎么走**（就是上面那行图）：扫描只是发现工具，它自己不下结论。看中的票必须
**加进信号池的 `universe`**（在「信号池」页点按钮即可，扫描表每行也有
一个 ＋；手工编辑 `config/universe.local.yaml` 同样有效），从此它的**卖出**信号才由
「每日信号」每天替你盯着。

> **不加进 `universe` 的票，没有任何人管它的卖出。** 这是本项目最容易踩的坑：
> 扫描报了 BUY，人买进去了，然后再也没有任何提示——因为「每日信号」只看固定池。
""")

_WORKFLOW = _f("""1. **收盘之后**再跑。baostock 的当日日线约 **{data_ready}** 之后才更新，
   早跑会因"全部标的数据未更新"直接退出（退出码 1）；实践中 **{safe_time}** 之后最稳。
2. 去「任务控制台」点「全市场扫描」的 ▶ 开始。**第一次先把 `--limit` 填 300 试跑**，
   确认环境正常再上全量——全量是挂机任务，不是等一会儿就好的那种。
3. 扫完在卡片里直接看信号表，或去「今日信号」页页尾看。**核对标题里的扫描日期**
   是不是你要的那天（停牌日、忘跑的日子最容易把上周的当成今天的）。
4. 挑出想跟的票 → 在扫描表那一行点 **＋**（或去「信号池」页搜名字加），
   改动落在本地 `config/universe.local.yaml`（你的数据，不进 git），
   命令行运行同样生效。
5. 此后**每天**收盘后跑一次「每日信号」，买卖提示都从这里出。
6. 改了策略参数就跑一次「回测」，看这套参数在历史上是什么形状。

**各任务耗时（实测，不是估计）**

| 任务 | 实测 | 备注 |
|---|--:|---|
| 全市场扫描（全量、缓存已热） | {scan_seconds}（**{scan_minutes}**） | {scan_pool}，报出 {scan_signals} 信号。**首次冷缓存要慢得多**，按 {scan_cold}准备：缓存省的是不重拉历史窗口，省不掉每票一次联网往返 |
| 回测（两个策略、缓存已热） | **{backtest_seconds}** | 首次要联网拉 {universe_n} 只标的约十年日线，会久一些 |
| 每日信号 | 十几秒**量级** | 只增量补当日数据。数据没更新时约 4 秒就明确退出 |

任务跑在**独立进程**里：关掉浏览器、甚至停掉本面板都不会中断它，回来还能看到进度。
同时只允许一个任务（baostock 是单会话，并发会互踢下线，还更慢）。
""")

_SCAN = _f("""| 列 | 含义 | 怎么用 |
|---|---|---|
| 日期 | 信号所属的**交易日** | 先核对它，不一定是今天 |
| 代码 / 名称 | 标的 | 代码按字符串处理，`000333` 的前导零不会被吃掉 |
| 策略 | 哪个策略报的 | 两个策略同时报同一只，只说明两套规则今天都满足，**不代表信号更强** |
| 收盘价 | 当日收盘价 | — |
| 涨跌幅% | 当日涨跌幅 | **参考价值最低的一列。** 机械信号看的是突破与量，不是今天涨了几个点 |
| 成交额 | 当日成交额（元） | 扫描已按 20 日均额 ≥ {liquidity_floor}（`scan.min_avg_amount`）滤掉低流动性标的 |
| **放量倍数** | 当日成交额 ÷ 前 20 日均额 | **最该看的一列**，也是表里唯一画成进度条的一列 |

**为什么放量倍数排在涨跌幅前面**：突破要靠量确认。没有量的新高多半是**假突破**
——盘口薄、几笔就能顶上去，第二天原样掉回来。涨跌幅只告诉你"今天涨了"，
放量倍数才告诉你"有多少钱在参与"。

**两个机械规则看不出来的陷阱**

- **同板块的多只信号，本质是同一笔押注。** 一次扫描报了 8 只银行，那不是 8 个
  独立机会，是"银行今天集体动了"这**一件事**。全买等于把仓位全押在一个 beta 上，
  风险不是分散了而是**集中了**。扫描按成交额排序，完全不知道行业。
- **扫出来 ≠ 策略在这只票上历史有效。** 回测只跑固定 `universe` 那 {universe_n} 只。
  某只票被扫出来，含义仅仅是"技术条件今天满足了"，没有任何历史背书。
- 那次全量实测在 {scan_pool} 里报了 {scan_signals}。这不是选股结论，是**待研究清单**。
""")

_METRICS = _f("""| 指标 | 含义 | 读的时候注意 |
|---|---|---|
| 总收益率 | 期末净值 / 期初 − 1 | 单独看没意义，**必须**和同期「等权买入持有」比 |
| 年化收益率 | 日历年化（首末跨度 ÷ 365.25） | 是几何平均，不是"每年都能有这么多" |
| 最大回撤 | 净值从历史高点跌下来的最大幅度 | **恒为负**，表里是绿色。这是"能不能睡着"的指标，比收益更决定你拿不拿得住 |
| 夏普比率(rf=0) | 年化收益 / 年化波动 | 无风险利率按 0 算（简化口径），中国实际约 {rf_real}，所以这里的夏普**偏高** |
| 交易次数 | 已成交笔数（买 + 卖） | 次数多不等于勤奋，等于**成本高**：每笔都吃佣金、印花税与 {slippage} 滑点 |
| 胜率 | 盈利平仓笔数 / 已平仓笔数 | 只统计**已平仓**的。40% 出头很正常——趋势跟随靠少数大赢单赚钱 |
| 盈亏比 | 总盈利 / 总亏损（取绝对值） | 必须和胜率配对看：胜率 40% + 盈亏比 2.5 是健康的，胜率 40% + 盈亏比 1.0 是稳定亏钱 |
| 平均持仓天数 | **自然日**，不是交易日 | 双均线明显长于唐奇安，那就是"迟钝"与"急躁"的数字形态 |

**本项目的实测结论（如实写，这是诚实结果不是 bug）**

两套口径并列。先看 **v0.1 基线（无叠加层）**：区间 {backtest_start} ~ {backtest_end}，
{universe_n} 只主板蓝筹，本金 {capital}，含全部成本。历史结论留档，永不重算：

| | 总收益 | 年化 | 最大回撤 | 夏普(rf=0) | 交易次数 |
|---|--:|--:|--:|--:|--:|
| 双均线交叉 ma_cross | {ma_total} | {ma_cagr} | {ma_dd} | {ma_sharpe} | {ma_trades} |
| 唐奇安通道突破 donchian | {dc_total} | {dc_cagr} | {dc_dd} | {dc_sharpe} | {dc_trades} |
| **等权买入持有** | **{hold_total}** | **{hold_cagr}** | **{hold_dd}** | — | — |
| 沪深300（价格指数） | {csi300_total} | {csi300_cagr} | {csi300_dd} | — | — |

**两个策略都大幅跑输"买入后躺平"**（{ma_total} / {dc_total} vs {hold_total}），
但最大回撤只有它的**约 1/3**（{ma_dd} / {dc_dd} vs {hold_dd}）。

**为什么，以及为什么这不是 bug**：这 {universe_n} 只是**十年长牛的蓝筹**，
恰恰是对趋势跟随**最不利**的样本——每一次震荡都把持仓洗出场，而躺平的人一次都没被洗掉。
别拿它去推断策略优劣——这组数字证明的是流水线跑得通、数是对的。

再看 **v0.4 当前默认（三策略 + 双叠加层开）**：同池同本金，区间终点更新到 {v4_end}：

| | 总收益 | 年化 | 最大回撤 | 夏普(rf=0) | 交易次数 |
|---|--:|--:|--:|--:|--:|
| 双均线交叉 ma_cross | {v4_ma_total} | {v4_ma_cagr} | {v4_ma_dd} | {v4_ma_sharpe} | {v4_ma_trades} |
| 唐奇安通道突破 donchian | {v4_dc_total} | {v4_dc_cagr} | {v4_dc_dd} | {v4_dc_sharpe} | {v4_dc_trades} |
| 时序动量 tsmom | {v4_ts_total} | {v4_ts_cagr} | {v4_ts_dd} | {v4_ts_sharpe} | {v4_ts_trades} |
| **等权买入持有** | **{v4_hold_total}** | **{v4_hold_cagr}** | **{v4_hold_dd}** | — | — |
| 沪深300（价格指数） | {v4_csi300_total} | {v4_csi300_cagr} | {v4_csi300_dd} | — | — |

没看错：**叠加层把三个策略的总收益全部打到了个位数到两成之间**。
止损与趋势过滤不是免费的保险，下面这张归因表就是保费单
（同池同区间的对照重跑，每格一次完整回测，总收益）：

| 总收益 | 无叠加层 | 只开趋势过滤 | 只开止损 | 双开（当前默认） |
|---|--:|--:|--:|--:|
| ma_cross | {v4_ma_base} | {v4_ma_gate} | {v4_ma_stop} | {v4_ma_total} |
| donchian | {v4_dc_base} | {v4_dc_gate} | {v4_dc_stop} | {v4_dc_total} |
| tsmom | {v4_ts_base} | {v4_ts_gate} | {v4_ts_stop} | {v4_ts_total} |

- **趋势过滤的保费**：长牛里每次回踩 200 日线都被强制离场、修复后高位接回，
  成本照付一整轮。
- **止损的保费对慢信号是灾难**：止损全在下跌里触发（定义如此），而"止损后封锁到
  基础信号重新 0→1"对 250 日动量意味着卖在暴跌底部后长期缺席——tsmom 单开止损时
  每只票都有一次一年以上的空窗（最长 {v4_lockout_max}），{v4_ts_base} 直接打到
  {v4_ts_stop}。donchian 自带的 10 日出场比 3×ATR 更早触发，止损对它几乎无感。
- **保费没换来多少保额**：ma_cross 回撤原地不动（{v4_ma_dd}），donchian 反而更深
  （{dc_dd} → {v4_dc_dd}），只有 tsmom 真降了（{v4_ts_base_dd} → {v4_ts_dd}）。
  一样**不是 bug**：保险的价值样本是持续单边下行，在十年长牛池里权重太小。

沪深300 只有 {csi300_total}（新终点 {v4_csi300_total}），是因为它是**价格指数、
不含成分股分红**，每年天然吃亏 {csi300_drag}——**不要拿它当主要参照**。主对照组是
用后复权价（分红再投资口径）算的「等权买入持有」，与策略同权，才是公平的那一栏。
""")

_STRATEGIES = _f("""| | 双均线交叉 ma_cross | 唐奇安通道突破 donchian | 时序动量 tsmom |
|---|---|---|---|
| 进场看什么 | 短均线**站上**长均线（看两条线的**相对位置**） | 收盘**创 N 日新高**（看**突破**） | **过去约 12 个月的收益为正**（今天 vs 250 根前，只看这两个时点） |
| 看不看量 | **不看** | **必须放量**才算，量不够的突破直接不认 | **不看** |
| 反应速度 | 迟钝：均线得先拐过来，往往涨了一截才进场 | 快：突破当天就进 | 最迟钝：一年的收益转正才动 |
| 出场 | 短均线**跌回**长均线下方，同样迟钝 | 跌破 M 日新低就走，很急 | 一年收益转负就走 |
| 实测交易次数（无叠加层基线，看性格用这行） | {ma_trades} 笔 | {dc_trades} 笔——差不多是前者两倍，成本也是 | —（基线表没有它） |
| 实测交易次数（当前默认，双叠加层开） | {v4_ma_trades} 笔 | {v4_dc_trades} 笔 | {v4_ts_trades} 笔——**最钝的策略反而最多**：多出来的全是叠加层的强制进出，见「怎么读回测指标」一节的归因表 |
| 性格 | 拿得久、被洗得少，但入场晚、回撤来了走得慢 | 抓得快，也被**假突破**打得多 | 最钝也最省心：中途的震荡一概不理 |
| **证据强度** | 经典**教学样品**：教科书常用，公开证据谈不上强 | 经典**教学样品**：海龟法则的入场部分，同上 | **★★★** 三者里最强：Moskowitz/Ooi/Pedersen (2012) 跨 58 个品种、上百年数据 |

一句话对比：**双均线是慢性子，唐奇安是急性子，时序动量是最钝的那个**。
前两者同一段行情里，前者少做多拿、后者多做少拿；实测总收益前者高
（无叠加层基线口径：{ma_total} vs {dc_total}），但回撤前者反而略深
（{ma_dd} vs {dc_dd}），因为它出场慢。

**为什么只有三个、为什么是这三个**：MACD / KDJ / 单纯 RSI 这类教科书信号，单独使用
扣除成本后没有稳定超额收益，所以本项目**不做**。时序动量是唯一有大样本、跨品种、
跨百年证据的入场规则，所以补上了它；另两个留着，因为它们是理解"趋势跟随"
最好的两个样品（一个看相对位置、一个看突破）。

参数都在 `config/settings.yaml` 的 `strategies` 段，改完跑一次回测就能看到形状变化。
**注意**：这三组参数**没有做过调优也没做样本外检验**（无参数扫描、无前进分析），
现有数字只是"流水线跑得通"的证据，不是策略有效性的证据。
""")

_CUSTOM = _f("""想把自己的想法接进来，一共**两步**——写一个类、注册一行。三个入口
（回测 / 每日信号 / 全市场扫描）、两个叠加层、面板的下拉框全都会自动认识它，
不用改任何别的地方。

**第一步：写策略类**（放在 `src/quant/strategy/` 下，比如 `my_break.py`）：

```python
import pandas as pd

from quant.strategy.base import Strategy


class MyBreak(Strategy):
    name = "my_break"      # 内部键：会进产物文件名、CSV、交易日志，定了就永不改
    label = "我的突破"      # 显示名：面板与报告给人看的，随时可改

    def __init__(self, n: int = 30):
        # 构造期校验用 raise 而非 assert（python -O 会把 assert 剥掉）。
        # 不校验的代价全是静默的：参数写错往往不报错，而是产出一串"合法"的假仓位。
        if isinstance(n, bool) or not isinstance(n, int) or n < 1:
            raise ValueError("参数 n 必须是不小于 1 的整数，实际为 " + repr(n))
        self.n = n

    def generate_positions(self, df: pd.DataFrame) -> pd.Series:
        # df 已清洗对齐，含 open/high/low/close/volume/amount 与后复权 adj_* 列。
        # 返回与 df.index 对齐的 0/1 序列：1 = 目标持有，0 = 目标空仓。
        high_n = df["adj_close"].rolling(self.n).max().shift(1)  # 前 n 日最高，不含当日
        return (df["adj_close"] > high_n).astype(int)
```

**第二步：注册**——`src/quant/strategy/__init__.py` 顶部 import，`REGISTRY` 字典里加一行：

```python
    "my_break": MyBreak,
```

想让它默认参与，就再往 `config/settings.yaml` 的 `strategies` 段加参数：

```yaml
strategies:
  my_break:
    n: 30
```

注册完即全线生效：`--strategy my_break` 能跑，面板回测页与「今日信号」的来源筛选
自动出现「我的突破」，交易日志的来源合法值自动多一席——这些全部从 `REGISTRY`
派生，**没有第二处要登记的名单**。

**三条硬规则**（违反了不会报错，只会给你一串"看起来能赚钱"的假回测）：

1. **只用当日及以前的数据**。`shift(-1)`、全样本 min/max 归一化、`rolling` 完
   忘了错位，都是未来函数。引擎的错位测试（信号 T 收盘 → T+1 开盘成交）能抓住
   入口层的偷看，但抓不住策略内部的——写完先问自己一句：**这根 K 线收盘那一刻，
   这个值真的算得出来吗？**
2. **价格一律用 `adj_close`（后复权）**。raw close 在除权日有断崖：10 送 10 之后
   价格腰斩，你的"跌破均线"可能只是一次分红。成交量、成交额没有复权问题，照用。
3. **"前 N 日"窗口自己 `shift(1)`**。`rolling(n)` **含当日**：`收盘 > rolling(n).max()`
   永不成立（自己不会大于含自己的最大值），得到的是**全 0 的死信号**——回测照样
   跑完、零告警、零成交。唐奇安第一版就踩过这个坑，现在有回归测试钉着。

写完跑一次回测（{backtest_seconds}），到「个股 K 线」页把 ▲▼ 买卖点标注肉眼过一遍
——它比任何指标都先告诉你"信号跟想的不一样"。
""")

_LIMITS = _f("""看结果之前先看这里。下面每一条都会让数字偏离真实交易：

- **涨跌停判定是近似**：按「开盘价 ≥ 昨收 × 1.095（或 ≤ 0.905）」或「当日最高 == 最低
  的一字板」推断。真实盘口的排队、封板中途打开、部分成交都复现不了。
- **除权除息按"分红再投资"处理**：除权日持仓股数 × (今日因子 / 昨日因子)，现金不变。
  于是**持仓股数会变成非整数**，与真实账户"现金分红入账、股数不变"有出入。
- **夏普按 rf = 0 算**（简化口径），中国无风险利率约 {rf_real}，真实夏普比这里低。
- **沪深300 是价格指数、不含成分股分红**，每年天然劣势 {csi300_drag}，别拿它当主要参照。
- **扫描池只有沪深主板非 ST**：创业板/科创板 20%、ST 的 5% 涨跌幅分支都没建模，
  所以刻意不扫，免得算出一堆不成立的信号。
- **成交假设过于友好**：以次日开盘价 ± {slippage} 滑点**全额**成交，不考虑冲击成本、
  流动性不足与部分成交。
- **股票池有幸存者偏差**：这 {universe_n} 只是**今天**回头挑出来的白马，
  2016 年时并不知道它们会走成这样；退市股、掉队股一只都没有，回测收益天然偏乐观。
- **胜率 / 盈亏比 / 平均持仓天数只统计已平仓交易**，回测末尾仍持仓的那笔不计入。
- **叠加层参数（k=3、n=200）与策略参数一样没调优、没做样本外检验**；止损"封锁到
  基础信号重新 0→1"的语义对慢信号尤其苛刻（tsmom 实测最长一次空窗 {v4_lockout_max}）。
  开不开、怎么调，拿「怎么读回测指标」一节的归因表做决定，别凭直觉。
- **数据没有与交易所对账**：baostock 是免费源，复权因子、停牌标记、成交额都按它给的用。

完整清单（含 ST 的 5% 未建模、现金不计利息、夏普年化 √252 的偏差等）见 README 的
「已知近似与局限」一节。
""")

_SAFETY = _f("""**这个面板能在本机执行脚本**——「开始」按钮起的是真进程，等于一个本地命令执行入口。

- **启动时必须显式绑定回环地址**：

  ```bash
  .venv/bin/streamlit run app/dashboard.py {bind_flag}
  ```

  **不要指望默认值是安全的。** 实测（`lsof -nP -iTCP:{default_port} -sTCP:LISTEN`）：
  `streamlit run` 不带地址参数时监听的是 `*:{default_port}`，也就是**所有网卡**
  ——启动日志里那两行 `Network URL` / `External URL` 就是证据。同一网段的人打开这个
  地址就能点你的「开始」按钮，等于能在你的机器上执行命令。加上 `{bind_flag}` 之后
  监听项变成 `127.0.0.1:{default_port}`，那两行 URL 也随之消失。
- 更不要用 `--server.address 0.0.0.0` 或任何内网地址——那是把上面这件事从疏忽变成主动邀请。
- 面板刻意**不暴露 `--config`**（避免任意路径读取），任务一律用 `config/settings.yaml`。
- 参数走白名单：日期必须是 `YYYY-MM-DD` 的真实日期、`--limit` 是 1–{limit_max} 的正整数、
  策略只能从注册表里挑；命令一律以**参数列表**方式执行，**不经过 shell**。
- 策略与信号仅用于学习，**不构成任何投资建议**。回测是历史模拟，含上面那一堆近似。
""")

_USERDATA = _f("""这个仓库里有两类文件，分得很清楚：

| | 是什么 | 进 git 吗 |
|---|---|---|
| `journal/trades.csv` | **你的交易记录**（不可再生） | **不进**。一份完整的真实交易记录，仓库一推到任何远端就全公开了 |
| `journal/trades.csv.bak` | 上一版日志，每次写盘前自动另存 | **不进**，同上 |
| `config/universe.local.yaml` | **你的信号池**（暴露持仓与关注标的） | **不进** |
| `config/settings.yaml` | 项目配置：成本模型、策略参数、`scan` 段 | **进**。这些是项目决策，改动值得留痕、也有测试钉着 |

`config/settings.yaml` 里也有一个 `universe`，但它只是**种子**
（新装机器的默认池子）。一旦 `config/universe.local.yaml` 存在，生效的就是后者
——「信号池」页顶部会直接写明当前池子来自哪个文件。删掉本地那份即可回到默认池子。

**改自己的池子、记一笔账，`git status` 都不会再变脏**，这也是分开放的直接好处。

**没有 git 兜底了，那靠什么撤销**：日志每次写盘前自动把上一版另存为
`journal/trades.csv.bak`（只留一层）。改错删错就从那里取回。长期备份用「记账」页的
**导出**（CSV / Excel）——导出的格式与日志文件完全相同，可以直接放回去。

**换新机器怎么办**：`git clone` 只会给你代码，**不会**带上面那三份文件。
把老机器上的 `journal/trades.csv` 与 `config/universe.local.yaml` **拷**过去即可
（放在同样的相对路径下）。不拷也能跑：日志从空表开始，池子从种子开始，
但你以前的记录不会自己出现——这一点必须先知道，别等到那时才发现。
""")

_CLI = _f("""面板上的按钮与下面这些命令**完全等价**——面板起的就是它们。
所有命令都要在**项目根目录**执行（脚本里的路径都是相对的）。

```bash
# 全市场扫描（收盘后跑）
.venv/bin/python scripts/run_market_scan.py                     # 全量，{scan_minutes}
.venv/bin/python scripts/run_market_scan.py --limit 300         # 只扫前 300 只（试跑）
.venv/bin/python scripts/run_market_scan.py --date 2026-08-21   # 补扫某个历史交易日

# 每日信号（收盘后跑）
.venv/bin/python scripts/run_daily_signal.py

# 回测
.venv/bin/python scripts/run_backtest.py                        # 全部策略，{backtest_seconds}
.venv/bin/python scripts/run_backtest.py --strategy donchian    # 只跑一个
.venv/bin/python scripts/run_backtest.py --refresh              # 丢弃缓存全量重拉
```

两点差别要知道：

- 命令行起的任务**不写** `output/runs/`，面板看不到它的进度（产物照样出现在页面上）；
- 因此它**不参与面板那条互斥**。终端已经在跑扫描时别再去面板点「开始」，
  否则两个 baostock 会话互踢下线。要么全在面板点，要么全在终端跑。

面板起的任务日志留在 `output/runs/logs/<run_id>.log`，**不自动清理**（留着事后排查）。
""")

SECTIONS: tuple[Section, ...] = (
    Section("tasks", "三个任务分别是什么", _TASKS,
            flow=("全市场扫描：发现新 BUY", "人工研究",
                  "加进信号池（universe.local.yaml）",
                  "每日信号：每天跟踪买/卖")),
    Section("workflow", "典型工作流", _WORKFLOW),
    Section("scan", "怎么读扫描结果", _SCAN),
    Section("metrics", "怎么读回测指标（含本项目实测结论）", _METRICS),
    Section("strategies", "三个内置策略的思路对比", _STRATEGIES),
    Section("custom", "自定义策略：写一个接进来", _CUSTOM),
    Section("limits", "已知局限", _LIMITS),
    Section("userdata", "你的数据在哪（哪些文件不进 git）", _USERDATA),
    Section("safety", "安全提示", _SAFETY, emphasis=True),
    Section("cli", "命令行等价用法", _CLI),
)

_BY_KEY = {s.key: s for s in SECTIONS}


def section(key: str) -> Section:
    """按 key 取一节。拼错必须 KeyError 响亮失败：返回 None 会在页面上渲染出
    一片空白，谁都不会注意到少了一节。"""
    return _BY_KEY[key]


# ---------------------------------------------------------------- 说明分成四页（v0.5.0 §5）
#
# 十节 10931 字堆在一页，实测渲染出 9598 字——是第二重那一页（控制台 4059）的 2.4 倍，
# 全站三分之二的文字压在同一屏。按**读者是谁**拆成四页：日常操作的人只需要第一页，
# 另外三页是"读一次就放着"的参考。
#
# 拆的是**呈现**，不是内容：SECTIONS 一个字不改、一节不重排（它的键与顺序被
# tests/test_guide.py 逐字钉着，Section.key 还是全项目定位一节的手段）。
# 这里只在其上加一层索引。

@dataclass(frozen=True)
class GuidePage:
    """说明手册的一页。

    `section_keys` 里的键拼错 = import 期 KeyError（见 _PAGE_SECTIONS），
    响亮失败而不是在页面上少渲染一节——后者没人看得出来。
    `lead` 是目录里那行副标题，回答"我什么时候需要点进去"。
    """
    key: str
    title: str
    url_path: str
    icon: str
    lead: str
    section_keys: tuple[str, ...]


GUIDE_PAGES: tuple[GuidePage, ...] = (
    GuidePage("guide", "使用说明", "guide", ":material/menu_book:",
              "三个任务怎么配合、扫描结果怎么读——日常只需要这一页",
              ("tasks", "workflow", "scan")),
    # metrics 与 strategies 必须同页：strategies 一节直接引用前者的归因表
    GuidePage("guide_metrics", "读懂回测", "guide-metrics", ":material/insights:",
              "指标各是什么意思，以及本项目跑出来的实测结论（含跑输躺平那件事）",
              ("metrics", "strategies")),
    GuidePage("guide_custom", "自定义策略", "guide-custom", ":material/code:",
              "把自己的想法接进来：模板、注册两步、三条不报错的坑",
              ("custom",)),
    # limits 末句「完整清单见 README」与安全提示、数据归属、命令行同属"读一次就放着"
    GuidePage("guide_limits", "边界与安全", "guide-limits", ":material/shield:",
              "已知局限、你的数据在哪、安全提示、命令行等价用法",
              ("limits", "userdata", "safety", "cli")),
)

#: {页 key: 该页要渲染的 Section 元组}。在 import 期就把键解析成对象——
#: 拼错的键当场 KeyError，而不是等到用户点开那一页才发现少了一节。
_PAGE_SECTIONS: dict[str, tuple[Section, ...]] = {
    p.key: tuple(_BY_KEY[k] for k in p.section_keys) for p in GUIDE_PAGES
}

# 分页必须**不重不漏**地盖住 SECTIONS。这条在 import 期就查：漏一节意味着某段正文
# 从面板上彻底消失，而页面照常渲染、没有任何报错——正是本项目一路在防的静默失败。
_paged = [k for p in GUIDE_PAGES for k in p.section_keys]
if sorted(_paged) != sorted(s.key for s in SECTIONS):
    raise RuntimeError(
        f"GUIDE_PAGES 没有不重不漏地盖住 SECTIONS：分页 {sorted(_paged)}，"
        f"实际 {sorted(s.key for s in SECTIONS)}")
del _paged

_BY_PAGE_KEY = {p.key: p for p in GUIDE_PAGES}


def guide_page(key: str) -> GuidePage:
    """按 key 取说明手册的一页。拼错同样 KeyError 响亮失败。"""
    return _BY_PAGE_KEY[key]


def page_sections(key: str) -> tuple[Section, ...]:
    """某一页要渲染的那几节，顺序与 SECTIONS 里一致。"""
    return _PAGE_SECTIONS[key]


# ---------------------------------------------------------------- 控制台的流水线区（v0.5.0 §6）
#
# 与 SECTIONS 里那张闭环图（"tasks" 一节的 flow）**刻意同一套词**：说明页讲的闭环
# 与控制台摆的顺序必须是同一件事，两处各写一套迟早各说各话。
CONSOLE_FLOW = ("全市场扫描：发现新 BUY", "人工研究",
                "加进信号池", "每日信号：每天跟踪买/卖")

#: 流水线区标题下那句。要防的是把编号读成"每天从头跑到尾"——三步的**频率不同**。
CONSOLE_FLOW_NOTE = _f(
    "三步的频率不一样：扫描按需跑（一次 {scan_minutes}），"
    "加进信号池是看到好票才做的人工动作，每日信号是每个交易日收盘后都该跑的那个。")

#: 「研究工具」区标题下那句。回测不在链上——这是它单独成区的全部理由。
CONSOLE_TOOLS_NOTE = ("回测是历史检验，**不产生今天的信号**，也不在上面那条链上。"
                      "改了策略参数想看形状变化时跑它。")

#: 人工步骤卡片的 `?` 帮助。
PIPELINE_MANUAL_HELP = (
    "**这一步没有按钮，因为它不是脚本能替你做的事。**\n\n"
    "扫描只报「技术条件今天满足了」，不报「这只票值得买」。中间这一步是你自己"
    "看基本面、看行业、看有没有踩雷——机械规则完全不知道这些。\n\n"
    "决定跟踪某只票之后，在「今日信号」页的扫描表里点 ＋，或到「信号池」页搜索添加。"
    "**不加进池子的票没有任何人管它的卖出信号。**")

#: 人工步骤卡片正文那句（比 ? 里的短，讲的是同一件事的结论）。
PIPELINE_MANUAL_NOTE = "扫描只报「技术条件满足」，值不值得跟踪要你自己判断。"


# ---------------------------------------------------------------- 就地帮助（§3.2）

# 任务卡片右上角 st.popover("?") 的内容：这个任务做什么、大概多久、产物在哪。
# 参数含义不写在这里——那是每个控件自己 help= 的事（见 PARAM_HELP），
# 写两遍必然改一处漏一处。键必须与 quant.runner.jobs.JOBS 完全一致（有测试钉住）。
JOB_HELP: dict[str, str] = {
    "market_scan": _f("""**全市场扫描**——固定池之外的雷达。

- **做什么**：扫全部沪深主板非 ST 股票（实测扫描池 {scan_pool}），
  **只报当日新触发的 BUY**，按当日成交额降序。
- **多久**：全量缓存已热实测 {scan_seconds}（{scan_minutes}）；
  首次冷缓存按 {scan_cold}准备。当挂机任务，随时可以点停止。
- **什么时候跑**：收盘后，{data_ready} 之后（当日数据那时才更新）。
- **产物**：`output/scan/<交易日>.csv`（全量），也会显示在「今日信号」页页尾。
  试跑（填了「只扫前 N 只」）写到 `<交易日>_limit{{N}}.csv`，**不会覆盖**同一天的全量结果；
  每份产物旁边还有一个 `.meta.json` 记着这趟扫了多少只，就是标题旁那个「全量/试跑」徽标的来源。
"""),
    "daily_signal": _f("""**每日信号**——跟踪固定池，这是唯一会提示你**卖出**的任务。

- **做什么**：只看信号池的 `universe`（本地 `config/universe.local.yaml`，
  没有它时用 `config/settings.yaml` 里的种子），与前一交易日比对，
  报**新增**的 BUY / SELL。
- **多久**：通常十几秒（只增量补当日数据）——这一项是**量级估计**，不像另两个任务有实测样本。
- **什么时候跑**：收盘后，{data_ready} 之后；建议每个交易日都跑。
  「今日无新信号」是绝大多数日子的正常结果。
- **产物**：`output/signals/<交易日>.csv`，显示在「今日信号」页。
"""),
    "backtest": _f("""**回测**——历史检验，不产生今天的信号。

- **做什么**：拿配置里的策略在固定池上跑一遍历史，出绩效指标、净值报告与逐笔成交。
- **多久**：两个策略、缓存已热实测 {backtest_seconds}；
  首次要联网拉 {universe_n} 只标的约十年日线，会久一些。
- **什么时候跑**：改完 `config/settings.yaml` 的策略参数之后。不依赖当日数据，随时能跑。
- **产物**：`output/<策略名>_<时间戳>/`（metrics.json / report.html / trades.csv /
  kline_*.html 等），显示在「回测报告」与「个股K线」两页。
"""),
}

# 参数控件的 help= tooltip。键是 (任务名, 参数名)，必须覆盖 JOBS 里每个 Param
# （有测试钉住）。四个控件都能"留空"，而留空的语义各不相同——不写清楚，
# 用户第一次点开始就会误跑一次全量扫描。
PARAM_HELP: dict[tuple[str, str], str] = {
    ("market_scan", "limit"): _f(
        "只扫前 N 只，**试跑用**（先填 300 确认环境正常）。"
        "留空 = 全量 {scan_pool}，{scan_minutes}起。上限 {limit_max}。"),
    ("market_scan", "date"): _f(
        "以指定交易日为基准，用来补扫历史某天。留空 = 最近交易日。"
        "给非交易日、或当日 {data_ready} 前数据还没更新，都会明确提示后退出。"),
    ("backtest", "strategy"): _f(
        "只回测一个策略：ma_cross（双均线交叉）、donchian（唐奇安通道突破）"
        "或 tsmom（时序动量）。"
        "留空 = 跑配置里的**全部**策略，每个出一份独立报告。"),
    ("backtest", "refresh"): _f(
        "丢掉本地**缓存**、把 {universe_n} 只标的约十年日线全部**重拉**一遍。"
        "只在怀疑缓存脏了时勾——平时增量补最近几天就够，勾上会慢很多。"),
}

# 每张数据表格上方那一行灰字。只讲"这张表里哪一列最该看 / 哪一列为什么是空的"，
# 逐列解释在说明页里，这里越短越好。
TABLE_HINTS: dict[str, str] = {
    "trades": "金额与股数已加千分位、数字列右对齐。盈亏只在**平仓**那一笔上有值，"
              "买入行与仍持仓的标的是空值——不是数据丢了。",
    "skipped": "被引擎约束挡掉的订单（涨跌停无法成交、T+1、资金不足一手等）。"
               "「信号有但没成交」先查这张表的原因列。",
    "scan": _f("最该看「放量倍数」（当日成交额 ÷ 前 20 日均额，画成进度条那列）"
               "——没有量的突破多半是假突破；涨跌幅的参考价值反而最低。"),
    "signal": "方向列 buy = 进场信号、sell = 出场信号；只有固定池 `universe` 里的"
              "标的才会出现在这里（扫描发现的票不加进池子就没人管卖出）。",
    "universe": "这张表就是「每日信号」每天替你盯的那批标的。最新价读自本地缓存"
                "（`data/cache/`），没取过数的标的显示 —（意思是还没拉过数据，"
                "不是价格为 0）。",
    "journal": "这张表可以**直接改**：改完按「保存修改」才落盘，勾上「删除」再保存才删。"
               "改动按记录号定位，所以筛选之后改也不会串行。",
    # 「最新价读自缓存、拿不到就显示 — 不按 0 算」交给 journal_ui 里那两列的
    # 列 help——它就挂在那两列的表头上，比表格上方的灰字位置更准。
    "positions": "由日志按先进先出配对算出来的，不是券商数据。",
    "by_source": "**照系统信号做的交易 vs 自己拍脑袋做的**，各自的胜率与盈亏比。"
                 "分组按当初**建仓**的来源——问的是当初为什么进场，不是为什么出场。",
    "closings": "一次卖出吃掉两个买入批次就是两行。「买入记录号」指回日志里那一笔，"
                "配对关系查得到；未配对的股数不计入盈亏（上面会有告警）。",
}

# ---------------------------------------------------------------- 信号池页（v0.2.2 §3.4）
# 底部那行小字：说清"改动落到哪里"。这是本页唯一会**改文件**的地方，
# 不写明白就会有人以为面板上的池子和命令行是两套东西。
# v0.3.2：落点从 settings.yaml 换成了本地覆盖文件（用户数据与项目配置分离）。
POOL_NOTE = ("改动直接写入本地文件 `config/universe.local.yaml`（**不受版本控制**，"
             "只属于这台机器；`config/settings.yaml` 逐字节不动）。")


def pool_source_note(source, local_path) -> str:
    """当前池子来自哪个文件——写在池子表格上方那行灰字里。

    **必须说得出来**：settings.yaml 与 universe.local.yaml 里都有 `universe`，
    看不出生效的是哪个的话，用户会以为自己在跟踪 A 池子、而三个脚本每天在跑 B 池子，
    且没有任何一处报错（正是本项目一路在防的那类故障）。

    `source` 是 `quant.config.universe_source` 的返回值：本地文件的路径，
    或 None（还没有本地文件，用的是 settings.yaml 里的默认池子）。
    """
    if source is None:
        return ("当前是 `config/settings.yaml` 里的**默认池子**（新装机器的起步值，"
                f"随代码一起分发）。第一次增删会在 {local_path} 生成属于你的那份，"
                "从此以它为准。")
    return (f"当前池子来自**本地文件** {source}（你的数据，不受版本控制、不会被提交）。"
            "删掉它即可回到 `config/settings.yaml` 里的默认池子。")


# 「加入」候选清单的来源与代价。必须提前说：这是一次联网请求，不是本地筛选。
POOL_LOAD_HINT = _f("候选清单 = 全市场扫描池（沪深主板、非 ST、上市满 400 天，"
                    "实测 {scan_pool}），来自 baostock 全市场查询。"
                    "跑过全市场扫描的话，本地 `data/symbols.parquet` 里 7 天内的"
                    "那份会直接复用（瞬时）；没有或已过期才联网拉，约 {pool_fetch}，"
                    "之后 6 小时内走缓存。只想移除标的的话不必加载。")

# ---------------------------------------------------------------- 交易日志页（v0.3.0 §5）
# 这一节的文案有一个别处没有的职责：**说清系统算不出什么**。日志页会给出持仓与
# 已实现盈亏，而用户会拿它跟券商 App 对账——对不上的时候，如果页面没提前说过
# 「配股没自动处理」「浮动盈亏依赖本地缓存」，用户只会认为整套数字都不可信。
# 这些句子与 README 的「交易日志」一节逐字对应（有测试钉住）。

JOURNAL_KINDS = """**这一页记的是你真实成交的每一笔**，据此算当前持仓与已实现盈亏。
它回答的问题和回测不同：回测说的是"机械规则在历史上表现如何"，
这里说的是"**你本人的决策质量如何**"。

四种记录类型，缺一种盈亏就会静默算错：

| 类型 | 含义 | 必填 | 对持仓 | 对盈亏 |
|---|---|---|---|---|
| **买入**（buy） | 买入成交 | 股数、成交价 | 加股数，建一个成本批次 | — |
| **卖出**（sell） | 卖出成交 | 股数、成交价 | 减股数，按先进先出消耗批次 | 产生已实现盈亏 |
| **股数调整**（adjust） | 送股/转增/拆股导致股数变化 | 股数（送股填正、缩股填负） | 加减股数，**成本总额不变**（摊薄单位成本） | — |
| **现金分红**（dividend） | 分红到账 | 到账金额 | 不变 | 计入已实现收益 |

后两种一年只用几次，收在录入区下面的「其他记录」里。但它们**不是可有可无**：
送股不是交易，你的股数却确实变了——不记的话持仓数与券商对不上，
此后每一笔卖出的先进先出配对全错，**而且不会报错**，只会给出一个看着正常的错数字。
分红同理：A 股蓝筹年化股息 2–4%，长期持有下静默忽略它是系统性低估。
"""

JOURNAL_LIMITS = """**已知局限**（看数字之前先看这里）：

- **浮动盈亏依赖本地行情缓存**（`data/cache/`）。没缓存的标的最新价与浮动盈亏
  显示 —，不按 0 算。想让它有数就先跑一次回测或每日信号（会把日线拉到本地）。
- **未处理配股、要约收购等复杂公司行为**。遇到时用「股数调整」+ 备注手工记，
  并自己复核一遍成本。
- **不做税务口径，也不构成任何税务建议**。费用只算佣金与印花税（与回测同口径），
  红利税之类请按券商实际扣款填进「佣金」栏。
- 持仓与盈亏是**按你记的日志**算出来的，不是券商数据。漏记一笔，后面全错——
  所以卖超、读不懂的行都会在持仓上方持续告警，别忽略它们。
"""

JOURNAL_SOURCE_HELP = ("这一笔当初**为什么进场**：照哪个策略的信号做的，还是自己拍的。"
                       "记满一段时间后，「按来源对比」那张表会告诉你哪一类更赚钱"
                       "——这是这套系统到底帮没帮上你的唯一诚实答案。"
                       "默认是「自主决策」，别把自己拍的记成信号。")

JOURNAL_STOP_HELP = ("入场时**计划**在哪里认输（可空）。事后可自查有没有守住自己的计划"
                     "——不守纪律是散户亏损的头号原因，而这是唯一能把它变成可测量数据的办法。")

JOURNAL_OTHER_HINT = ("送股/转增/拆股只改股数、**不改成本总额**（正确口径是摊薄单位成本）；"
                      "现金分红只填税后到账金额，不动持仓批次。两者都不记的话，"
                      "持仓会与券商对不上，而系统不会报错。")

JOURNAL_EDIT_HINT = ("改了股数或成交价，金额与费用**不会**自动重算"
                     "——它们是你实际付出的钱，只有你知道券商扣了多少。"
                     "删除要勾选后再按保存（两步是刻意的：这份文件不可再生，"
                     "误删只能从下面那份上一版备份里取回）。")


def journal_backup_note(backup_path) -> str:
    """历史表旁那行小字：撤销手段在哪。

    日志自 v0.3.2 起**不受版本控制**（它是用户数据，一推远端就公开了），于是
    "误删一行 `git checkout` 找回"这条路没了。替代品是每次写盘前自动另存的上一版。
    **必须写在页面上**：一个没人知道的备份文件等于没有备份——真出事的那一刻，
    用户只会以为数据没了，而它就躺在日志旁边。
    """
    return ("这份日志与它的备份都**不受版本控制**（是你的数据，不会被提交，"
            "也不会随仓库泄露出去）。每次写盘前，**上一版**会自动另存为 "
            f"`{backup_path}`；改错删错了就从那里取回。"
            "只留一层（不是历史版本库），长期备份请用下面的导出。")


JOURNAL_EXPORT_HINT = ("导出的就是上面那张表——**当前筛选后的结果**，所见即所得。"
                       "CSV 带 BOM，Excel 双击打开中文不乱码，格式与 `journal/trades.csv` "
                       "完全相同（可以直接放回去）。")

JOURNAL_PNL_HINT = ("胜率、盈亏比、平均持仓天数**与回测报告用同一套口径**"
                    "（同一个函数算的），所以可以把这里的数字和「回测报告」页并排比。"
                    "一次卖出吃掉两个买入批次算两笔。平手（不赚不亏）计入亏损那一边。")

JOURNAL_EMPTY = ("还没有任何记录。在上面「记一笔」填一笔真实成交即可；"
                 "也可以在「今日信号」页的表格里点「＋ 记一笔」，代码、日期、方向"
                 "与来源都会替你填好。")

# 空态文案（§3.2）：按钮在哪 + 等价命令 + 预计耗时。
# "暂无数据"四个字什么也没告诉人，用户下一步只能去猜是坏了还是没跑过。
EMPTY_STATES: dict[str, str] = {
    "backtest": _f("暂无回测结果 —— 点上方控制条的「▶ 开始」，"
                   "或在项目根目录跑 `python scripts/run_backtest.py`"
                   "（缓存已热约 {backtest_seconds}；首次要联网拉 {universe_n} 只标的"
                   "约十年日线，会久一些）。"),
    "signal": _f("暂无信号记录 —— 收盘后（{data_ready} 之后当日数据才更新）"
                 "点上方控制条的「▶ 开始」，"
                 "或跑 `python scripts/run_daily_signal.py`（约十几秒）。"),
    "scan": _f("暂无全市场扫描结果 —— 收盘后点上方控制条「全市场扫描」的「▶ 开始」，"
               "或跑 `python scripts/run_market_scan.py`"
               "（全量 {scan_pool}，缓存已热实测 {scan_minutes}，"
               "冷缓存按 {scan_cold}准备；先用 `--limit 300` 试跑更稳）。"),
}
