"""交易日志（v0.3.0）：记录真实发生的每一笔交易，据此算持仓与已实现盈亏。

系统此前只能回答"机械规则在历史上表现如何"，回答不了"**你本人的决策质量如何**"。
这个包补上后一半：`schema` 定字段与校验、`store` 管落盘、
`pnl` 做 FIFO 配对与持仓盈亏，（后续里程碑）`export` 做导出。

分层约定同项目其余部分：这里全是纯函数 + 文件读写，面板只负责渲染与编排。
"""
